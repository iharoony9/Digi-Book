## Packages
date-fns | Formatting dates for transactions and bills
@hookform/resolvers | Form validation

## Notes
- Currency should always be displayed as whole numbers with " Rupees" suffix.
- Print styles are added to index.css to hide the sidebar and header when printing bills.
- Global Account Balance is stored in settings with key 'account_balance'.
